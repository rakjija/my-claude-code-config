## TypeScript 설정

- strict 모드: **true** / false
- 타입 정의: **interface** (객체) / type (유니온/튜플)
- any 사용: **금지** / 허용
- 타입 단언: as / **타입 가드**
- non-null assertion (!): 허용 / **최소화**
- enum: 사용 / **as const 객체 선호**
- import type: **사용** / 미사용
